---
layout: layouts/base.njk
title: "Name Index"
---

{% set sidebar_items = [
  { label: "Ancestors", url: "/people/" },
  { label: "Families", url: "/families/" },
  { label: "Index", url: "/index/" }
] %}

<div class="section-layout">
  {% include "partials/section-sidebar.njk" %}

  <div class="section-content">
    <div class="page-header">
      <h1>Name Index</h1>
      <p class="page-intro">
        Every name that appears anywhere on this site, alphabetically, with links to wherever it can be read: a full profile, an entry on someone else's page, or a mention in a research note, journal entry, or family narrative.
      </p>
    </div>

    {% for entry in peopleIndex | sortedPeopleIndex %}
    <section class="index-entry" id="{{ entry.slug }}">
      <h2 class="index-entry__name">{{ entry.name }}</h2>
      {% if entry.tags %}
      <div class="index-entry__tags">
        {% for tag in entry.tags %}
        <span class="badge badge--{{ tag }}">{{ tag | replace("-", " ") }}</span>
        {% endfor %}
      </div>
      {% endif %}
      <ul class="index-entry__appearances">
        {% for a in entry.appearances %}
        <li><a href="{{ a.url }}">{{ a.label }}</a></li>
        {% endfor %}
      </ul>
    </section>
    {% endfor %}

  </div>
</div>